#!/usr/bin/env zsh
set -e
setopt extended_glob

git branch -D master

git checkout -b master

roots compile

rm -rf *~public~.git

mv public/* .

rm -rf public

git add --all .

git now

git push -f origin master

